add_subdirectory(geom)
find_library(IRRLICHT Irrlicht)
find_file(IRRLICHT_HEADER irrlicht/irrlicht.h)
message("Irrlicht: ${IRRLICHT} ${IRRLICHT_HEADER}")
if (IRRLICHT)
    add_subdirectory(irrlicht)
else()
    message(WARNING "Irrlicht library not found, skipping test.")
endif()
